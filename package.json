{
  "name": "firesale",
  "version": "1.0.0",
  "description": "simple markdown editor",
  "main": "app/main.js",
  "dependencies": {
    "electron": "^2.0.0",
    "marked": "^0.3.6"
  },
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
